#ifndef TCPL_GETLINE_H__
#define TCPL_GETLINE_H__

#define MAX_N 1000
int getline1(char* line, int maxlen);
void copy(char* from, char* to);
void reverse(char* str, int len);
int strindex(char* str, char* substr);


#endif
